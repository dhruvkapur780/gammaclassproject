# Diya Patel — Personal Site

Static, single-page profile site generated from LinkedIn information.

## Tech stack

- HTML and CSS only (no build step)

## Development

- Open `index.html` directly in a browser, or
- Serve the folder with any simple HTTP server (for example, in this directory run `python -m http.server 8000` and visit `http://localhost:8000`).

